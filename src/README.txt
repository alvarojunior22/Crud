# 🛒 CRUD de Productos con Vite + JSON Server + Fetch

Esta aplicación permite **agregar, editar, eliminar y listar productos** en una tabla dinámica, con los datos almacenados en un archivo `db.json` mediante una API REST simulada con **JSON Server**.

---

## ✨ Características

- Interfaz simple con HTML, CSS y JavaScript
- Conexión real con archivo `db.json`
- Funciones `async/await` y `fetch`
- Totalmente funcional sin backend real
- Código modular con Vite

---

## 🚀 Requisitos

- Node.js y npm
- `json-server` instalado globalmente

```bash
npm install -g json-server


para uso dirigete a la terminal para activar la url de nuestro JSON es con el siguiente Código
json-server --watch db.json --port 3000

escribe:
npm run dev y recibe el locastholt y pegalo en el navegador para visualizarlo
